name: re_ucm_author_today
description: "Plugin that adds Author.Today support to ReUCM"
version: 1.0.0
homepage: "https://reucm.pages.dev"
repository: https://github.com/BooksFine/re_ucm_author_today
publish_to: none

environment:
  sdk: ^3.6.0-334.2.beta
  flutter: ">=1.17.0"

dependencies:
  dio: ^5.7.0
  flutter:
    sdk: flutter
  flutter_inappwebview: ^6.1.5
  flutter_mobx: ^2.2.1+1
  flutter_svg: ^2.0.10+1
  json_annotation: ^4.9.0
  mobx: ^2.3.3+2
  path_provider: ^2.1.4
  re_ucm_core:
    git:
      url: https://github.com/BooksFine/re_ucm_core.git
  retrofit: ^4.4.1
  sembast: ^3.7.5

dev_dependencies:
  build_runner: ^2.4.13
  flutter_lints: ^5.0.0
  json_serializable: ^6.8.0
  mobx_codegen: ^2.6.1
  retrofit_generator: ^9.1.2

flutter:
  assets:
    - assets/logo.svg
